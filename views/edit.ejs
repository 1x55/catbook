<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Cat</title>
</head>
<body>
    <div>
        <ul>
            <li>
                <a href="/">Home</a>
            </li>
            <li>
                <a href="/upload">Upload</a>
            </li>
        </ul>
    </div>

    <div>
        <div>
            <div>
                <!-- upload: creates a brand new entry  -->
                <h2>Upload a Cat</h2>
                <!-- ideally would like to use PUT for upadated but forms only have GET amnd POST -->
                <form action="/edit/<%= cat._id %>" method='POST'>
                    <div> 
                        <label for="name">Cat's name</label>
                        <!-- required allows for validation on the fontend -->
                        <input type="text" id="name" name="name" value="<%= cat.name %>" required>
                    </div>

                    <div> 
                        <label for="age">Age</label>
                        <input type="number" id="age" name="age" value="<%= cat.age %>"required>
                    </div>

                    <div> 
                        <label for="favoriteFood">Favortite Food</label>
                        <!-- required allows for validation on the fontend -->
                        <input type="text" id="favoriteFood" name="favoriteFood" value="<%= cat.favoriteFood %>"required>
                    </div>

                    <div> 
                        <label for="funFact">Fun Fact</label>
                        <input type="text" id="funFact" name="funFact" required>
                    </div> 

                    <!-- image using forms: set input type to file
                    <div> 
                        <label for="image">Image</label>
                        <input type="file" id="image" name="image" required>
                    </div>  -->
  
                    <!-- button -->
                    <button type="submit">Submit</button>
                    
                </form>
            </div>
        </div>
    </div>
    
</body>
</html>