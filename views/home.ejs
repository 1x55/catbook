<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cat Gallery</title>
    <!-- add Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <!-- add stylesheet -->
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
</head>

<body>
 
    <%- include('partials/navbar')%>

  <div class="container mt-4">
    <div class="row">
    <!-- EJS is expecting a object cats, sent from catControllers, and then loop through each one -->
    <% cats.forEach(cat => { %>
        <div class="col-lg-3 col-md-6 col-sm-6 mb-4"> <!-- Bootstrap classes to create four cards per row -->
            <div class= "card h-100">
                <img class="card-img-top" src="/images/<%= cat.image %>" alt="Card image cap">
            <div class="card-body">
                <h5 class="card-title"><%= cat.name %></h5>
                <p class="card-text">
                    Age: <%= cat.age %> <br>
                    Favorite Food: <%= cat.favoriteFood %> <br>
                    Fun fact: <%= cat.funFact %>
                </p>
            </div>    
            <div class="card-footer">
                <a href = "/edit/<%= cat._id%>" class="btn btn-primary">Edit</a>
                <form action="/delete/<%= cat._id%>" method = "POST" style="display:inline;">
                <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div> 
    </div>
     <% }); %> <!-- Add this closing tag for the forEach loop -->
    </div>
 </div>
</body>
</html>